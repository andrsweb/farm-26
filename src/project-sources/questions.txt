# Хедер
1) В бургер меню корзина. В счетчике - что если будет 100, 1000?? это же будет большой счетчик(зеленый индикатор), который перекроет иконку.
-- Решено(да/нет)
2) Не сильно ли маленький отступ в хедере справа?(десктоп). Как раз скролл же справа.
-- Решено(да/нет)
3) Кривая иконка телефона справа. В хедер экшнс. Пиксель перфект мимо. Меньше на 1-2 пикселя по факту.
-- Решено(да/нет)
4) Должно ли закрываться бургер меню, при переходе на новую страницу? или это будет подгрузка аяксом?
-- Решено(да/нет)
5) Если хедер фиксед - почему нет скролла в дизайне? Иначе фиксед цеплять по скроллу уже. И как без скролла это представляется вообще? Это я за бургер меню.
-- Решено(да/нет)
6) В нав-меню добавил 2 доп класса. Пришлось.
-- Решено(да/нет)