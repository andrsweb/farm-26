# Хедер
1) В бургер меню корзина. В счетчике - что если будет 100, 1000?? это же будет большой счетчик(зеленый индикатор), который перекроет иконку. Так же, какого
хрена для счетчика другой шрифт? Тянуть шрифт ради 1 цифры??? И там еще где-то есть момент по шрифту. Предлагаю вообще по доп шрифтам для 1 слова пропустить этот
момент. Это шляпа.
-- Решено(да/нет)
2) Не сильно ли маленький отступ в хедере справа?(десктоп). Как раз скролл же справа.
-- Решено(да/нет)
3) Кривая иконка телефона справа. В хедер экшнс. Пиксель перфект мимо. Меньше на 1-2 пикселя по факту.
-- Решено(да/нет)
4) В нав-меню добавил 2 доп класса. Пришлось.
-- Решено(да/нет)

# Хиро(каталог)
1) Карточки. Изображения я скачал вместе с фоном. Иначе это тупо хардкод каждой карточки, то есть глупости. А мы глупости не делаем.
   + куча доп настроек понадобится впшных, если следовать логике диза. То есть - позиция картинки, сайзы, цвет фона и тд.
-- Решено(да/нет) 
2) Дизайней-хитрец решил обмануть. Но кого только?:))) В мобилке у последней карточки(так как текст длинный и он судя по всему не влез) фонт сайз отличается от
   компонента. на 2 пикселя. Я сделал 12 пикселей в мобилке всем карточкам. Эти приколы тоже не нужны. От компонентов только 1 слово. Можно было мин-хейт задать
   кнопкам. Но в дизайне очевидно используется фиксированная высота кнопки карточки категории.
-- Решено(да/нет)